{
  "name": "PROG8850 DB Automation",
  "updateContentCommand": "sudo apt-get update && sudo apt-get install -y mysql-client && python -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "postAttachCommand": {
    "activate-venv": "/bin/bash -lc 'echo \"source .venv/bin/activate\" >> ~/.bashrc'"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "github.vscode-github-actions"
      ]
    }
  }
}
